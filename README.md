Para iniciar o projeto, use dois terminais: 
1° terminal: pasta base, rode o comando npm run dev (após ter instalado o npm via npm install)
2° terminal: vá até a pasta /routes e rode o seguinte comando uvicorn api:app --reload --port 8000
